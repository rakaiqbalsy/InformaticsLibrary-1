<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelompok3.rplinformatika.informaticslibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".SplashActivity.SPLASHActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BookActivity.MainActivityBook" />
        <activity android:name=".BookActivity.BookActivity" />
        <activity android:name=".JurnalActivity.MainActivityJurnal" />
        <activity android:name=".JurnalActivity.JurnalActivity" />
        <activity android:name=".LoginAndRegister.RegisterActivity" />
        <activity android:name=".LoginAndRegister.LoginActivity" />
        <activity android:name=".Bantuan.BantuanActivity"/>
        <activity android:name=".Dashboard.DashBoard"></activity>
    </application>

</manifest>